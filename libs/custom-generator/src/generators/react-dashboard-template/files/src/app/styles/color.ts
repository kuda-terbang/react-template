import { Theme } from "@mui/material";
import { red } from '@mui/material/colors';

const colors = {
  primary: {
    main: '#556cd6',
  },
  secondary: {
    main: '#19857b',
  },
  error: {
    main: red.A400,
  },
} as Theme['palette']

export default colors